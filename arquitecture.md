# Project Setup Directive: Spring Boot + React + SQLite

## 1. Role and Objective
Act as a senior full-stack developer. Your task is to initialize a monorepo containing a Java Spring Boot backend, a React frontend, and an SQLite database. 

Execute the setup step-by-step. Do not skip configuration files. Wait for confirmation between major steps if your context window requires it, otherwise output the exact terminal commands and file contents to build this structure.

## 2. Project Architecture
Create the following root structure:
```text
project-root/
├── .gitignore
├── README.md
├── backend/       # Spring Boot application
└── frontend/      # React application (native)
